
export default function (context) {
    
    // context.userAgent = process.server ? context.req.headers['user-agent'] : context.navigator.userAgent

    // if(/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(context.userAgent)) {
    //     context.store.state.userType = 'Phone'
    // }else {
    //     context.store.state.userType = 'Desktop'
    // }
  }
